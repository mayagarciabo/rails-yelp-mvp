<div class="container mt-4">
  <% @reviews.each do |review| %>
      <p><%= review.content %></p>
      <p><%= review.rating %></p>
  <% end %>

  <%= render "reviews/form", review: @review %>
</div>
